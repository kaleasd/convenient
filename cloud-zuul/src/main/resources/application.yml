# 应用名称
server:
  port: 9100

spring:
  application:
    name: convenient-zuul

eureka:
  client:
    service-url:
      defaultZone: http://eureka-7900:7900/eureka/
  instance:
    hostname: localhost
    instance-id: convenient-zuul
